# Rust
/target
Cargo.lock.bak
**/*.rs.bk

# IDE
.vscode/
.idea/
*.swp

# Claude Code local state
.claude/settings.local.json

# Local config / user data
config.local.toml

# Per-user runtime state (window position, current aircraft, toggles).
# config.toml + pronunciation.toml are shared; settings.toml is local.
settings.toml

# Models and large assets (downloaded separately)
/models/*.bin
/models/*.gguf
/models/*.onnx
/models/piper/piper.exe
/models/piper/*.dll
/models/piper/voices/
/models/piper/cache/
/models/piper/espeak-ng-data/

# Build artifacts
*.exe
*.pdb
# Portable distribution bundles produced by scripts/release packaging.
/release/

# OS
Thumbs.db
Desktop.ini
.DS_Store
